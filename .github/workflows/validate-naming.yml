name: validate-naming

on:
  push:
    branches:
      - test-branch

jobs:
  validate-naming:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate project names (.project files) for kebab-case
        id: validate_projects
        shell: bash
        continue-on-error: true
        run: |
          echo "🔍 STEP 1: Validating project names (.project files) for kebab-case..."
          violations=0

          is_kebab_case() {
            [[ "$1" =~ ^[a-z0-9]+(-[a-z0-9]+)*$ ]]
          }

          IFS=$'\n'
          project_files=($(find . -type f \( -name ".project" -o -name "*.project" \)))

          echo "🔎 Found ${#project_files[@]} .project files"

          for project_file in "${project_files[@]}"; do
            proj_name=$(grep -oPm1 '(?<=<name>)[^<]+' "$project_file" || echo "")

            if [[ -z "$proj_name" ]]; then
              echo "❌ ERROR: No <name> tag found in $project_file"
              echo "::error file=$project_file::Missing <name> tag"
              violations=$((violations + 1))
              continue
            fi

            if [[ "$proj_name" == *.module ]]; then
              core_name="${proj_name%.module}"
            elif [[ "$proj_name" == *.parent ]]; then
              core_name="${proj_name%.parent}"
            else
              core_name="$proj_name"
            fi
            
            # ⛔ Must start with "om-"
            if [[ ! "$core_name" =~ ^om- ]]; then
              echo "❌ ERROR: Project name '$proj_name' must start with 'om-' (found in $project_file)"
              echo "::error file=$project_file::Project name '$proj_name' must start with 'om-'"
              violations=$((violations + 1))
            fi
            
            # ⛔ Length check
            if (( ${#core_name} > 3 )); then
              echo "❌ ERROR: Project name '$proj_name' exceeds 3 characters (found in $project_file)"
              echo "::error file=$project_file::Project name '$proj_name' exceeds 3 characters (limit: 3)"
              violations=$((violations + 1))
            fi
            
            # ⛔ Kebab-case check
            if ! is_kebab_case "$core_name"; then
              echo "❌ ERROR: Invalid project name '$proj_name' (found in $project_file)"
              echo "::error file=$project_file::Invalid project name '$proj_name'. Expected kebab-case"
              violations=$((violations + 1))
            fi
          done
          
          # ✅ Success message if no violations
          if [[ "$violations" -eq 0 ]]; then
            echo "✅ All project names are valid (start with 'om-', are ≤ 3 chars, and in kebab-case)."
          fi

          echo "project_violations=$violations" >> "$GITHUB_OUTPUT"

      - name: Validate process, activity, variable, and schema names
        id: validate_processes
        shell: bash
        continue-on-error: true
        run: |
         echo "🔍 STEP 2: Validating naming conventions (processes, activities, variables, schemas)..."

            camel_case_regex='^[a-z]+([A-Z][a-z0-9]*)*$'
            pascal_case_regex='^[A-Z][a-z0-9]+([A-Z][a-z0-9]*)*$'
            special_char_regex='[%$@_#]'

            violations=0

            # ✅ PROCESS FILE (.bwp) checks
            mapfile -t bwp_files < <(find . -type f -name "*.bwp")
            echo "🔎 Found ${#bwp_files[@]} .bwp files"

            bwp_violations=0

            for file in "${bwp_files[@]}"; do
              filename=$(basename "$file")
              base="${filename%.bwp}"

              # Check process name
              if [[ ! "$base" =~ $camel_case_regex ]]; then
                echo "❌ ERROR: Process name '$base' must be camelCase (found in $file)"
                echo "::error file=$file::Invalid process name '$base'. Must be camelCase"
                violations=$((violations + 1))
                bwp_violations=$((bwp_violations + 1))
              fi

              if [[ "$base" =~ $special_char_regex ]]; then
                echo "❌ ERROR: Process name '$base' contains special characters (found in $file)"
                echo "::error file=$file::Special characters not allowed in process name '$base'"
                violations=$((violations + 1))
                bwp_violations=$((bwp_violations + 1))
              fi

              # ✅ ACTIVITY (task name) checks inside each .bwp
              echo "🔎 Checking activity (task) names in: $filename"
              mapfile -t tasks < <(grep -oP '<[^:>]*task\b[^>]*name="\K[^"]+' "$file")

              task_errors=0
              for task in "${tasks[@]}"; do
                if [[ ! "$task" =~ $pascal_case_regex ]]; then
                  echo "❌ ERROR: Activity name '$task' in $filename must be PascalCase"
                  echo "::error file=$file::Activity name '$task' must be PascalCase"
                  violations=$((violations + 1))
                  task_errors=$((task_errors + 1))
                fi
                if [[ "$task" =~ $special_char_regex ]]; then
                  echo "❌ ERROR: Activity name '$task' contains special characters"
                  echo "::error file=$file::Special characters not allowed in activity name '$task'"
                  violations=$((violations + 1))
                  task_errors=$((task_errors + 1))
                fi
              done

              if [[ ${#tasks[@]} -eq 0 ]]; then
                echo "ℹ️ No task activities found in $filename"
              elif [[ $task_errors -eq 0 ]]; then
                echo "✅ All activity names are valid in $filename and contain no special characters."
              fi

            done

            if [[ "$bwp_violations" -eq 0 ]]; then
              echo "✅ All .bwp process filenames follow camelCase and contain no special characters."
            fi

            # ✅ VARIABLE (.jsv / .msv) checks
            mapfile -t var_files < <(find . -type f \( -name "*.jsv" -o -name "*.msv" \))
            echo "🔎 Found ${#var_files[@]} variable files (.jsv/.msv)"

            var_violations=0
            jsv_violations=0
            msv_violations=0

            for vfile in "${var_files[@]}"; do
              filename=$(basename "$vfile")

              if [[ "$vfile" == *.jsv ]]; then
                vnames=$(grep -oP '<jobSharedVariable name="\K[^"]+' "$vfile" || true)
                var_type=".jsv"
              else
                vnames=$(grep -oP '<moduleSharedVariable name="\K[^"]+' "$vfile" || true)
                var_type=".msv"
              fi

              var_count=$(echo "$vnames" | grep -c . || true)

              if [[ "$var_count" -eq 0 ]]; then
                echo "📄 $filename ($var_type) contains 0 variables."
              else
                echo "📄 $filename ($var_type) contains $var_count variable(s):"
                echo "$vnames" | sed 's/^/   - /'
              fi

              while IFS= read -r var; do
                if [[ -z "$var" ]]; then continue; fi

                if [[ ! "$var" =~ $camel_case_regex ]]; then
                  echo "❌ ERROR: Variable '$var' in $filename must be camelCase"
                  echo "::error file=$vfile::Variable '$var' must be camelCase"
                  violations=$((violations + 1))
                  var_violations=$((var_violations + 1))
                  [[ "$var_type" == ".jsv" ]] && jsv_violations=$((jsv_violations + 1))
                  [[ "$var_type" == ".msv" ]] && msv_violations=$((msv_violations + 1))
                fi

                if [[ "$var" =~ $special_char_regex ]]; then
                  echo "❌ ERROR: Variable '$var' in $filename contains special characters"
                  echo "::error file=$vfile::Special characters not allowed in variable name '$var'"
                  violations=$((violations + 1))
                  var_violations=$((var_violations + 1))
                  [[ "$var_type" == ".jsv" ]] && jsv_violations=$((jsv_violations + 1))
                  [[ "$var_type" == ".msv" ]] && msv_violations=$((msv_violations + 1))
                fi
              done <<< "$vnames"
            done

            if [[ "$jsv_violations" -eq 0 ]]; then
              echo "✅ All variables in .jsv files follow camelCase and contain no special characters."
            fi

            if [[ "$msv_violations" -eq 0 ]]; then
              echo "✅ All variables in .msv files follow camelCase and contain no special characters."
            fi

          # ✅ SCHEMA (.xsd) checks
          mapfile -t xsd_files < <(find . -type f -name "*.xsd")
          echo "🔎 Found ${#xsd_files[@]} schema files (.xsd)"

          xsd_violations=0

          for xsd in "${xsd_files[@]}"; do
            filename=$(basename "$xsd")
            xname="${filename%.xsd}"

            if [[ ! "$xname" =~ $camel_case_regex ]]; then
              echo "❌ ERROR: Schema '$xname' must be camelCase"
              echo "::error file=$xsd::Schema filename '$xname.xsd' must be camelCase"
              violations=$((violations + 1))
              xsd_violations=$((xsd_violations + 1))
            fi
            if [[ "$xname" =~ $special_char_regex ]]; then
              echo "❌ ERROR: Schema name '$xname' contains special characters"
              echo "::error file=$xsd::Special characters not allowed in schema name '$xname'"
              violations=$((violations + 1))
              xsd_violations=$((xsd_violations + 1))
            fi
          done

          if [[ "$xsd_violations" -eq 0 ]]; then
            echo "✅ All .xsd schema filenames follow camelCase and contain no special characters."
          fi

      - name: Validate presence of .substvar files in META-INF folders for specific environments
        id: validate_substvar
        shell: bash
        continue-on-error: true
        run: |
          echo "🔍 STEP 3: Checking for .substvar files in META-INF folders..."

          violations=0
          envs="DEV4 IT UAT ST PT AM"

          # Step 1: Find all "META-INF" directories
          mapfile -t meta_dirs < <(find . -type d -name META-INF)

          # Step 2: Filter valid application directories (exclude paths like *.module)
          declare -A seen
          app_dirs=()
          for meta in "${meta_dirs[@]}"; do
            parent_dir=$(dirname "$meta")
            base=$(basename "$parent_dir" | tr '[:upper:]' '[:lower:]')
            if [[ "$parent_dir" == *".module"* ]]; then
              continue
            fi
            if [[ -z "${seen[$base]}" ]]; then
              seen[$base]=1
              app_dirs+=("$parent_dir")
            fi
          done

          echo "🔎 Applications to validate:"
          for app in "${app_dirs[@]}"; do echo "$app"; done

          # Step 3: Check for each expected .substvar file
          for app_dir in "${app_dirs[@]}"; do
            meta_inf="$app_dir/META-INF"
            echo "📦 Validating application: $app_dir"

            for env in $envs; do
              if find "$meta_inf" -type f -iname "$env.substvar" | grep -iq "$env.substvar"; then
                echo "✅ Found $env.substvar in $meta_inf"
              else
                echo "⚠️ Environment .substvar not found: $meta_inf/$env.substvar"
                echo "::warning file=$meta_inf/$env.substvar::Missing $env.substvar"
                violations=$((violations + 1))
              fi
            done
          done

          echo "substvar_violations=$violations" >> "$GITHUB_OUTPUT"
          
      - name: Validate required subfolders in Processes directory of the .module
        id: validate_process_structure
        shell: bash
        continue-on-error: true
        run: |
          echo "🔍 STEP 4: Validating Processes subfolders (Frontend, Backend, Orchestration)..."

          violations=0
          expected_subdirs=("Frontend" "Backend" "Orchestration")

          module_dir=$(find . -type d -name "*.module" | head -n 1)
          processes_dir="$module_dir/Processes"

          echo "📂 Checking: $processes_dir"

          for sub in "${expected_subdirs[@]}"; do
            sub_dir="$processes_dir/$sub"
            if [[ ! -d "$sub_dir" ]]; then
              echo "❌ ERROR: Missing subfolder '$sub' in $processes_dir"
              echo "::error file=$sub_dir::$sub folder missing in Processes"
              violations=$((violations + 1))
            fi
          done

          if [[ "$violations" -eq 0 ]]; then
            echo "✅ All Processes folders contain Frontend, Backend, and Orchestration subdirectories."
          fi

          echo "structure_violations=$violations" >> "$GITHUB_OUTPUT"
          
      - name: Validate REST path and parameter naming
        id: validate_rest_naming
        shell: bash
        continue-on-error: false
        run: |
          set -euo pipefail
          trap 'echo "❌ Script failed at line $LINENO with exit code $?."' ERR
          echo "🔍 Searching for module.bwm file under *.module/META-INF/ ..."
          bwm_file=$(find . -type f -path "*/META-INF/module.bwm" | grep "\.module/" | head -n1)

          if [[ -z "$bwm_file" ]]; then
            echo "❌ ERROR: No module.bwm file found under any *.module/META-INF/"
            exit 1
          fi
          echo "✅ Found BWM file: $bwm_file"

          echo "🔎 Checking for REST service bindings in $bwm_file ..."
          if ! grep -q 'scaext:binding[^>]*xsi:type="rest:RestServiceBinding"' "$bwm_file"; then
            echo "ℹ️ No REST service bindings found. Nothing to validate."
            exit 0
          fi
          echo "✅ REST service bindings found. Extracting info..."

          echo "DEBUG: Extracting component blocks..."
          mapfile -t component_blocks < <(
            awk '
              BEGIN { RS="</sca:component>"; ORS=RS }
              /<sca:component / { print }
            ' "$bwm_file"
          )
          echo "DEBUG: Total components found: ${#component_blocks[@]}"

          for component in "${component_blocks[@]}"; do
            # Extract processName from <scaext:implementation ... processName="...">
            process_name=$(grep -oP '<scaext:implementation[^>]*processName="\K[^"]+' <<< "$component" || echo "")
            [[ -z "$process_name" ]] && continue
            echo "Process Name: $process_name"

          # DEBUG: Show full component block if needed
          # echo "DEBUG: Component Block: $component"

          # Extract all <sca:service name="..."> entries
          echo "$component" | awk '
                BEGIN { RS="<"; FS=">" }
                $1 ~ /^sca:service/ {
                  if (match($0, /name="[^"]+"/)) {
                    name=substr($0, RSTART+6, RLENGTH-7)
                    print "  Service Name: " name
                  }
                }
              '
              echo
            fi
          done
      - name: Fail job if any violations found
        if: |
          steps.validate_projects.outputs.project_violations != '0' || 
          steps.validate_processes.outputs.process_violations != '0' || 
          steps.validate_substvar.outputs.substvar_violations != '0' ||
          steps.validate_process_structure.outputs.structure_violations != '0' ||
          steps.validate_rest_naming.outputs.rest_naming_violations != '0'
        run: |
          echo "❌ Validation errors detected. Failing the job."
          exit 1
