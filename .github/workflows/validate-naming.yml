name: Validate TIBCO BW6 Naming Conventions

on:
  push:
    branches:
      - main

jobs:
  validate-naming:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate project names for kebab-case
        shell: bash
        run: |
          echo "🔍 Starting TIBCO BW6 naming convention validation..."

          violations=0

          is_kebab_case() {
            [[ "$1" =~ ^[a-z0-9]+(-[a-z0-9]+)*$ ]]
          }

          IFS=$'\n'
          project_files=($(find . -type f \( -name ".project" -o -name "*.project" \)))
          echo "🔎 Found ${#project_files[@]} project files."

          for project_file in "${project_files[@]}"; do
            proj_name=$(grep -oPm1 '(?<=<name>)[^<]+' "$project_file" || echo "")
            if [[ -z "$proj_name" ]]; then
              echo "::error file=$project_file::No <name> tag found"
              violations=$((violations + 1))
              continue
            fi

            if [[ "$proj_name" == *.module ]]; then
              core_name="${proj_name%.module}"
            elif [[ "$proj_name" == *.parent ]]; then
              core_name="${proj_name%.parent}"
            else
              core_name="$proj_name"
            fi

            if ! is_kebab_case "$core_name"; then
              # Print error with annotation so GitHub highlights it
              echo "::error file=$project_file::Invalid kebab-case name '$proj_name' (core '$core_name')"
              violations=$((violations + 1))
            fi
          done

          if (( violations > 0 )); then
            echo "❌ Validation failed with $violations error(s). Please fix naming conventions."
            exit 1
          else
            echo "✅ All project names conform to kebab-case naming."
          fi
